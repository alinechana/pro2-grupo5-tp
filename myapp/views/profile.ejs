<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('./partials/head')%>

<body>
	<!-- Header -->
	<% if (locals.user != undefined) { %>
		<%- include('./partials/headerLogueado') %>
	<% } else { %>
		<%- include('./partials/header') %>
	<% } %>
	<!-- /Header -->

	<!-- Datos del usuario -->
	<section class="info-usuario container products-wrapper">
		<div class="row">
			<div class="img-container offset-3 col-2">
				<img class="imgProfile" src="/images/users/<%= usuario.foto%>" alt="">
			</div>
			<div class="data-container col-6">
				<div class="d-flex align-items-center ">
					<h1 class="main-title"> <%= usuario.email %> <!-- directamente llamo a la propiedad que ya defini en el controlador usuario.usuario -->
						<a href="./profile-edit"> <!-- NO EXISTE ESTE EJS -->
							<i class="fas fa-cog"></i>
						</a>
					</h1>
					<a class="btn mt-3 ml-4" href="">SEGUIR</a>
				</div>
				<div>
					<ul class="">
						<li> Productos publicados por el usuario: <%= totalProductos %></li>
						
					</ul>
				</div>
			</div>
		</div>
	</section>
	<!-- /Datos del usuario -->

	<!-- Mis productos -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Mis productos</h2>
			</div>
			<!-- Show mis productos -->
			<% for (let i = 0; i < productos.length; i++) { %>   <!-- FOR DE PRODUCTOS: -->
				<div class="col-12 col-sm-6 col-lg-3 divProd">
					<section class="product-box">
						<a href="/products/id/<%= productos[i].id %>"> 
						<figure class="product-box_image">
							<img src="/images/products/<%= productos[i].foto%>" alt="cafetera moulinex"> 
						</figure>
					
					<article class="product-box_data">
						<h2> <%= productos[i].nombre %> </h2>
						<p class="descripcion"> <%= productos[i].descripcion %> </p> 
						
					

					</article>
				</section>
				</div>
			<% } %>
	<!-- /Mis productos -->


	<!-- footer -->
	<%- include('./partials/footer') %>
	<!-- /footer -->
</body>	
</html>